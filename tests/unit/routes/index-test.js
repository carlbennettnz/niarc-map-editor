import { expect } from 'chai';
import { describeModule, it } from 'ember-mocha';

describeModule('route:index', 'IndexRoute', {}, function() {
  it.skip('loads the model from localstorage (needs localstorage mock)');
});
